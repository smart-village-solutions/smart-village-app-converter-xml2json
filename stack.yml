version: '3.7'

services:
  app:
    image: registry.gitlab.tpwd.de/ikusei/smart-village-app-converter-xml2json
    networks:
      - default
      - public
    environment:
      RAILS_ENV: production
      RAILS_LOG_TO_STDOUT: 1
    deploy:
      labels:
        traefik.enable: "true"
        traefik.port: 80
        traefik.docker.network: public
        traefik.main.frontend.rule: Host:tmb.bad-belzig.smart-village.app
        traefik.main.frontend.passHostHeader: "true"
        traefik.main.frontend.redirect.entryPoint: https
        traefik.api.frontend.rule: Host:tmb.bad-belzig.smart-village.app
        traefik.api.frontend.passHostHeader: "true"
        traefik.api.frontend.redirect.entryPoint: https

  db:
    networks:
      default:
      adminer:
        aliases:
          - bb-bad-belzig-tmb-importer

networks:
  adminer:
    external: true
  public:
    external: true
